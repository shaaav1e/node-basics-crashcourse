import os from 'os'; //gives info about your system - like memory, cpu, network, etc

// console.log(os.userInfo()); //gives user info

console.log(os.platform()); //gives platform

console.log(os.userInfo().username); //gives user info

console.log(os.totalmem()); //gives total memory

console.log(os.freemem()); //gives free memory

console.log(os.cpus()); //gives cpu info